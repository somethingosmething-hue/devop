[project]
name = "discord-lang-bot"
version = "0.1.0"
description = "Discord bot with Skript-like scripting language"
requires-python = ">=3.10"
dependencies = [
    "discord.py>=2.4.0",
    "PyNaCl>=1.5.0",
    "python-dotenv>=1.0.0",
    "watchdog>=4.0.0",
    "aiofiles>=23.0.0",
]
